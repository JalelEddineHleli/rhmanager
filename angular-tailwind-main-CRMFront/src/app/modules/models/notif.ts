
export interface Notif {
  length: number;

uuid :string;
  id: number;               // ID unique de l'utilisateur
  useruuid: string;            // Email de l'utilisateur
  message: string;        // Prénom
  timestamp: string;   
  isRead: boolean;       // Nom
}